name: Build TWRP Recovery

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y ccache bison build-essential curl flex g++-multilib gcc-multilib git gnupg gperf lib32ncurses5-dev lib32z1-dev libgl1-mesa-dev libxml2-utils xsltproc zlib1g-dev

      - name: Set up Repo Tool
        run: |
          mkdir -p ~/bin
          curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
          chmod a+x ~/bin/repo
          echo "$HOME/bin" >> $GITHUB_PATH

      - name: Initialize and Sync TWRP Source
        run: |
          mkdir twrp
          cd twrp
          repo init -u https://github.com/minimal-manifest-twrp/platform_manifest_twrp_aosp.git -b twrp-12.1 --depth=1
          repo sync -c -j$(nproc --all) --force-sync --no-clone-bundle --no-tags

      - name: Clone Device Tree
        run: |
          git clone https://github.com/${{ github.repository }} twrp/device/xiaomi/fire

      - name: Build TWRP
        run: |
          cd twrp
          source build/envsetup.sh
          lunch twrp_fire-eng || lunch twrp_fire-userdebug
          make recoveryimage -j$(nproc --all)

      - name: Upload TWRP Recovery Image
        uses: actions/upload-artifact@v4
        with:
          name: TWRP_Recovery_Fire
          path: twrp/out/target/product/fire/recovery.img
